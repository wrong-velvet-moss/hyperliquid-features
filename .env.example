# Copy to .env and adjust as needed. .env is gitignored.
#   cp .env.example .env

# TimescaleDB (Postgres)
POSTGRES_USER=hl
POSTGRES_PASSWORD=hl
POSTGRES_DB=hlsignals
POSTGRES_PORT=5432

# Grafana
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
GRAFANA_PORT=3000
